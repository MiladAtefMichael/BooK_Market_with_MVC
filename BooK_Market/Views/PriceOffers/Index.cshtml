@model IEnumerable<BooK_Market.Models.PriceOffers>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NewPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PromaotionText)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NewPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PromaotionText)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PriceOfersId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PriceOfersId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PriceOfersId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
